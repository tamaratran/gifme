{
  "name": "gifme-functions",
  "private": true,
  "description": "GifMe AI Cloud Functions — proxy for Replicate face-swap.",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@fal-ai/client": "^1.10.1",
    "ffmpeg-static": "^5.3.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1",
    "gifsicle": "^7.0.1"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
